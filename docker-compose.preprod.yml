services:
  crimpy-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dev-crimpy-frontend
    restart: unless-stopped
    labels:
      - traefik.enable=true
      - traefik.http.routers.dev-crimpy-frontend.rule=Host(`dev.crimpy.app`)
      - traefik.http.routers.dev-crimpy-frontend.entrypoints=websecure
      - traefik.http.routers.dev-crimpy-frontend.tls.certresolver=dnsResolver
      - traefik.http.routers.dev-crimpy-frontend.middlewares=traefik-ip
      - traefik.http.middlewares.traefik-ip.ipallowlist.sourcerange=10.14.0.0/16
    expose:
      - 3000
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0
      - PUBLIC_API_URL=https://devapi.crimpy.app
    networks:
      - proxy
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "require('http').get('http://localhost:3000', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  proxy:
    external: true
