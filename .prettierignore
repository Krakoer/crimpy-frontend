# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Vendored byte for byte from crimpy-backend, where nothing runs prettier. The
# contract CI job compares the bytes, so reformatting it here breaks the copy.
/contract/
